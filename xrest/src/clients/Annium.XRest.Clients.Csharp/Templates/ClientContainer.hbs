{{~ if usages.size > 0 ~}}
{{~ for ns in usages ~}}
using {{ ns }};
{{~ end ~}}

{{~ end ~}}
namespace {{ namespace }};

public class {{ type }}
{
    {{~ for client in clients ~}}
    public {{ client.type }} {{ client.name }} => new(_request);
    {{~ end ~}}
    private readonly IHttpRequest _request;

    internal {{ type }}(IHttpRequest request)
    {
        _request = request;
    }
}
