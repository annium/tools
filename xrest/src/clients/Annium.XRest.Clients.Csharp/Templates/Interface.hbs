public interface {{ if args_count > 0 }}{{ name }}<{{ args }}>{{ else }}{{ name }}{{ end }}{{ if has_extends }} : {{ extends }}{{ end }}
{
    {{~ for field in fields ~}}
    {{ field.type }} {{ field.name }} { get; }
    {{~ end ~}}
}
