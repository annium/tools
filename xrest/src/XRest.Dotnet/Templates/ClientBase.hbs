{{~ for ns in usages ~}}
using {{ ns }};
{{~ end ~}}

namespace {{ namespace }}
{
    public abstract class ClientBase
    {
        protected IHttpRequest Client => _requestFactory();
        private readonly RequestFactory _requestFactory;

        protected ClientBase(RequestFactory requestFactory)
        {
           _requestFactory = requestFactory;
        }
    }
}