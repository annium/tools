namespace Sample.Controllers.Deep;

// A second stand-in controller, so ApiModelBuilderTests can check that actions are grouped by their
// declaring type rather than by the order they arrive in.
internal class OtherController;
