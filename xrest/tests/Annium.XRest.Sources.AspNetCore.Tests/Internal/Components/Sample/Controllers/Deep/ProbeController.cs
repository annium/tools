namespace Sample.Controllers.Deep;

// Stands in for a real controller in ControllerModelBuilderTests: the builder keeps whatever follows
// the `Controllers` segment of the controller's namespace, so the fixture needs a segment after it.
internal class ProbeController;
