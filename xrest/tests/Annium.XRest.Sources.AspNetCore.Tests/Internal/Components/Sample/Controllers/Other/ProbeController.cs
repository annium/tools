namespace Sample.Controllers.Other;

// Same class name as Sample.Controllers.Deep.ProbeController, a different type: ApiModelBuilderTests
// uses the pair to pin that controllers are grouped by type rather than by name.
internal class ProbeController;
